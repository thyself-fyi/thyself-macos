{
  "name": "thyself-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute thyself-db --file=schema.sql --remote"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250310.0",
    "wrangler": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
